{
  "name": "notes-app-backend",
  "version": "1.0.0",
  "description": "backend for the notes application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "start-dev": "node -r dotenv/config app.js dotenv_config_path=./.env-dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cynergy",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^8.3.2"
  },
  "engines": {
    "node": "12.18.3"
  }
}
